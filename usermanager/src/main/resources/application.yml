spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/vue?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf-8
    driver-class-name: com.mysql.jdbc.Driver
mybatis-plus:
  #扫描映射文件
  mapper-locations: classpath:/mapper/**/*.xml
  #设置主键自增
  global-config:
    db-config:
      id-type: auto
server:
  port: 7000